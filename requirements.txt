# MLOps: Von der Entwicklung zum produktiven Einsatz
# Schulungs-Pack: Lab-Dependencies, Python 3.11
#
# Install:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# Lab 2 (Docker-Track) zieht torch/torchvision separat im Dockerfile vom CPU-Wheel-Index.

# --- Core ML Stack ---------------------------------------------------------
scikit-learn>=1.3,<2.0
numpy>=1.24,<3.0
pandas>=2.0,<3.0
matplotlib>=3.7,<4.0
scipy>=1.10,<2.0
pyyaml>=6.0,<7.0

# --- MLOps Tooling ---------------------------------------------------------
# mlflow >= 2.9 wegen Registry-Aliases (set_registered_model_alias); Stages sind ab 2.9 deprecated.
mlflow>=2.10,<3.0
# Evidently fest gepinnt: die API aendert sich zwischen Minor-Versionen. Die Lab-3-
# Syntax (evidently.report.Report / evidently.metric_preset) gilt fuer 0.4.x.
evidently==0.4.40

# --- Serving (Lab 2) -------------------------------------------------------
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.0,<3.0

# --- Optional / wahlweise --------------------------------------------------
# Lab 1 Übung F (DVC)
dvc>=3.0,<4.0

# Lab 3 Übung F (Fairness-Mini-Demo)
fairlearn>=0.10,<1.0

# Lab 2 Torch-Track (lokal, ohne Docker): manuell installieren
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install pillow>=10.0,<12.0

# --- Lab 4 (optional, Feast) -----------------------------------------------
# Lab 4 hat ein eigenständiges Python-3.12-venv unter labs/lab4_feast/.venv,
# weil feast eine engere Python-Range (3.10 bis 3.12) als das Hauptpack hat.
# Setup-Befehle siehe labs/lab4_feast/README.md. Nicht im Haupt-venv mischen.
